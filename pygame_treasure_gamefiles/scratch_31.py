import random

import pygame.display
import pygame.image
from pygame.examples.resizing_new import screen

pygame.font.init()
pygame.mixer.init()
pygame.display.init()
pygame.init()

# initializing imported module


# displaying a window of height
# 500 and width 400
pygame.display.init()
pygame.display.set_mode()
width = 400
height = 400

pygame.display.set_mode()
screen.set_caption('Treasure_chest_the_game')
black = (0, 0, 0)
white = (255, 255, 255)

clock = pygame.time.clock()
crashed = False
chestimg - pygame.image.load("/assets", "Treasure_Chest_set.png")


def chest(x, y):
    gamedisplay.blit(chestimg, (x, y))
    x = (display_width) * 10
    y = (display_height) * 10


staus = True
while (staus):
    while running:
        for event in pygame.event.get():
            if event.type == pygame.QUIT:
                running = False

                gamedisplay.fill(white)
                chest(x, y)

                pygame.display.update()
                clock.tick(60)


# game variables
class Treasure_Chest_The_Game:
    pass


pygame = ('Treasure_Chest_The_Game')
silver_coin = [5]
gold_coin = [25]
pygame.display.set_caption('Treasure_Chest_the_game!')
fps = 60
timer = pygame.time.Clock()
font = pygame.font.Font('freesansbold.ttf', 44)
smaller_font = pygame.font.Font('freesansbold.ttf', 37)
active = False
# win, loss,
records = [0, 0, 0]
player_score = 0
initial_deal = False
my_coins = []
outcome = 0
hand_active = False
outcome = 0
add_score: bool = False
results = ['Player WINS!' 'players lose']


# coins by selecting randomly from chest, and make function for one card at a time
class Lucky_number:
    pass


def deal_coins(current_hand, current_play, current_coin=5, ):
    coin = random.randint(1, 5, 26)
    return current_hand, current_coin

    def computer_out_put(game_values, ):
        if Lucky_number != '25':
            return gold_coin

    gold_coin == vars("25 == x")
    silver_coin == random.randit(1, 6)

    def fuction(Adding_values_game_logic):
        silver_coin = random.randit(1, 6)

    return

    def fcuntion(five_coins_Game_math, five_silver_coins=player_score):
        vars("25 == winner")
        vars(">25 == loser")
